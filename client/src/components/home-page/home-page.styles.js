import styled from "styled-components";

export const Styled = styled.div`
  .view-all {
    font-size: 90%;
    margin-left: 20px;
  }

  .sticky-header {
    position: sticky;
    top: 0;
    z-index: 9999;
  }
`;
